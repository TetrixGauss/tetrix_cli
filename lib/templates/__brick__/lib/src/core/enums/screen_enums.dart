enum SplashScreenStatus {
  initial,
  showLoginOptions,
  fetchingData,
  userAuthenticated,
  userNotAuthenticated,
  animationCompleted,
  userFetched,
}

enum RegistrationStatus { initial, successful }

enum ForgotPasswordStatus { initial, successful }

enum LoginStatus { initial, successful }

enum AdvicesStatus {
  initial,
}

enum AccountStatus {
  initial,
  successful,
}

enum AppointmentType {
  psychology,
  nutrition,
}

enum ConferenceBookingType {
  booking,
  purchase,
}
