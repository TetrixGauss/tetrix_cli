enum NetworkResponseStatus { ok, networkError, failed }

enum ToastStatus { successful, warning, error, info, initial, progress }

enum ConfigSettingsStatus { init, back, restart }

enum LoadingStatus { initial, loading, fetched, failed }

enum NotificationsStatus { init, fetched, failed }

enum CommonDataStatus { init, fetched, failed }

enum SnackBarType { none, success, clientError, serverError }
